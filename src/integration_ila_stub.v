`timescale 1ns / 1ps

// The current teammate DHT11 source references an ILA IP that is not checked in.
// This empty integration stub preserves that source verbatim and lets the
// non-debug build elaborate. Replace it with the generated ILA IP when needed.
module ila_0 (
    input        clk,
    input        probe0,
    input        probe1,
    input  [4:0] probe2,
    input  [5:0] probe3,
    input [39:0] probe4
);
endmodule
